{
  "name": "test",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "watch": "NODE_ENV=development rspack build --watch",
    "build": "NODE_ENV=development rspack build",
    "deploy": "rspack build"
  },
  "devDependencies": {
    "@rspack/cli": "^1.3.3",
    "@rspack/core": "^1.3.3",
    "css-loader": "^7.1.2"
  }
}
